# Codex 專用 Skill：從這裡開始

先讀 [README.md](README.md)。在解壓資料夾中核對雜湊並安裝：

```text
python -X utf8 verify_package.py
python -X utf8 install.py
python -X utf8 install.py --apply
```

安裝後重新載入 Codex Skills，確認原生 ChatGPT 對話工具存在。每台機器、每個專案使用自己的專用 Chat 並確認 6 Pro；同帳號登入不會自動安裝。

本機紀錄、備份與私人對話不包含在公開套件裡。安裝與還原操作、一次無機密往返驗收、資料範圍及已知限制均見 README。
